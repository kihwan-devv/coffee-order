"use client";
import type { User } from "@/types";
import { useOrderRooms } from "./order-room-provider";
export function UserSelector({ teamCode, teamName, members }: { teamCode: string; teamName: string; members: User[] }) { const { selectUser } = useOrderRooms(); return <main className="mx-auto flex min-h-screen max-w-md flex-col justify-center p-5"><div className="mb-8"><p className="text-sm font-bold text-amber-700">{teamName}</p><h1 className="mt-2 text-3xl font-black tracking-tight">누구신가요?</h1><p className="mt-3 text-sm leading-6 text-stone-500">본인을 선택하면 이 브라우저에서 자동으로 기억해요.</p></div><div className="space-y-3">{members.map((user) => <button key={user.id} onClick={() => selectUser(user.id, teamCode)} className="flex w-full items-center gap-4 rounded-3xl border border-stone-200 bg-white p-4 text-left shadow-sm transition hover:-translate-y-0.5 hover:border-amber-400"><span className="grid size-11 place-items-center rounded-full bg-amber-100 font-black text-amber-800">{user.name.slice(0, 1)}</span><span className="flex-1 font-bold">{user.name}</span><span className="text-xl text-stone-400">›</span></button>)}</div></main>; }
